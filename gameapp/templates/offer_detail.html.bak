{% extends "base.html" %}

{% block content %}
<div class="row">
    <div class="col-lg-8">
        <div class="card mb-4">
            <div class="card-body">
                <div class="d-flex align-items-center mb-3">
                    <span class="badge badge-game me-2">{{ offer.game.title }}</span>
                    <span class="badge bg-secondary me-2">{{ offer.server.name }}</span>
                    <span class="badge bg-warning text-dark">{{ offer.amount }} шт. в наличии</span>
                </div>

                <h1 class="mb-3">{{ offer.item_name }}</h1>

                <div class="d-flex align-items-center mb-4">
                    <div class="me-auto">
                        <span class="price-tag fs-3">{{ offer.price }} ₽</span>
                        <span class="text-muted ms-2">за 1 шт.</span>
                    </div>
                    <button class="btn btn-primary btn-lg">
                        <i class="fas fa-shopping-cart me-2"></i> Купить сейчас
                    </button>
                </div>

                <div class="mb-4">
                    <h5 class="fw-bold mb-3">Описание предмета</h5>
                    <p>{{ offer.description }}</p>
                </div>

                <div class="mb-4">
                    <h5 class="fw-bold mb-3">Характеристики</h5>
                    <div class="row">
                        <div class="col-md-6">
                            <p class="mb-2"><strong>Качество:</strong>
                                <span class="text-primary">{{ offer.get_quality_display }}</span>
                            </p>
                            <p class="mb-2"><strong>Редкость:</strong>
                                <span class="text-warning">{{ offer.get_quality_display }}</span>
                            </p>
                        </div>
                        <div class="col-md-6">
                            <p class="mb-2"><strong>Можно обменять:</strong>
                                {% if offer.is_tradable %}Да{% else %}Нет{% endif %}
                            </p>
                        </div>
                    </div>
                </div>

                <div class="alert alert-warning">
                    <h5 class="alert-heading"><i class="fas fa-exclamation-triangle me-2"></i>Важно!</h5>
                    <p class="mb-0">После оплаты свяжитесь с продавцом для передачи предмета. Все сделки защищены системой безопасных транзакций.</p>
                </div>
            </div>
        </div>

        <div class="card mb-4">
            <div class="card-header bg-light">
                <h5 class="mb-0"><i class="fas fa-comments me-2"></i>Отзывы о продавце</h5>
            </div>
            <div class="card-body">
                {% for review in reviews %}
                <div class="mb-3 pb-3 border-bottom">
                    <div class="d-flex align-items-center mb-2">
                        {% if review.reviewer.userprofile.avatar %}
                        <img src="{{ review.reviewer.userprofile.avatar.url }}" class="user-avatar me-2" alt="{{ review.reviewer.username }}">
                        {% else %}
                        <img src="https://via.placeholder.com/40" class="user-avatar me-2" alt="{{ review.reviewer.username }}">
                        {% endif %}
                        <div>
                            <strong>{{ review.reviewer.username }}</strong>
                            <div class="text-warning">
                                {% for i in "12345" %}
                                {% if forloop.counter <= review.rating %}
                                <i class="fas fa-star"></i>
                                {% else %}
                                <i class="far fa-star"></i>
                                {% endif %}
                                {% endfor %}
                            </div>
                        </div>
                    </div>
                    <p>{{ review.comment }}</p>
                    <small class="text-muted">{{ review.created_at|date:"d.m.Y H:i" }}</small>
                </div>
                {% empty %}
                <p class="text-muted">Пока нет отзывов</p>
                {% endfor %}
            </div>
        </div>
    </div>

    <div class="col-lg-4">
        <div class="card mb-4">
            <div class="card-header bg-light">
                <h5 class="mb-0"><i class="fas fa-user me-2"></i>Продавец</h5>
            </div>
            <div class="card-body">
                <div class="d-flex align-items-center mb-3">
                    {% if seller_profile.avatar %}
                    <img src="{{ seller_profile.avatar.url }}" class="user-avatar me-3" alt="{{ offer.seller.username }}">
                    {% else %}
                    <img src="https://via.placeholder.com/60" class="user-avatar me-3" alt="{{ offer.seller.username }}">
                    {% endif %}
                    <div>
                        <h5 class="mb-0">{{ offer.seller.username }}</h5>
                        <span class="badge {% if seller_profile.online_status %}badge-online{% else %}badge-offline{% endif %}">
                            {% if seller_profile.online_status %}online{% else %}offline{% endif %}
                        </span>
                    </div>
                </div>

                <div class="d-flex justify-content-between mb-3">
                    <div class="text-center">
                        <div class="fs-5 fw-bold">{{ seller_rating }}/5</div>
                        <div class="text-muted small">Рейтинг</div>
                    </div>
                    <div class="text-center">
                        <div class="fs-5 fw-bold">{{ reviews.count }}</div>
                        <div class="text-muted small">Отзывов</div>
                    </div>
                    <div class="text-center">
                        <div class="fs-5 fw-bold">{{ offer.seller.offer_set.count }}</div>
                        <div class="text-muted small">Продаж</div>
                    </div>
                </div>

                <button class="btn btn-outline-primary w-100 mb-2">
                    <i class="fas fa-comment me-2"></i> Написать сообщение
                </button>
                <button class="btn btn-outline-secondary w-100">
                    <i class="fas fa-star me-2"></i> Добавить в избранное
                </button>
            </div>
        </div>

        <div class="card mb-4">
            <div class="card-header bg-light">
                <h5 class="mb-0"><i class="fas fa-shield-alt me-2"></i>Безопасная сделка</h5>
            </div>
            <div class="card-body">
                <ul class="list-unstyled">
                    <li class="mb-2"><i class="fas fa-check text-success me-2"></i> Гарантия возврата средств</li>
                    <li class="mb-2"><i class="fas fa-check text-success me-2"></i> Защита от мошенников</li>
                    <li class="mb-2"><i class="fas fa-check text-success me-2"></i> Поддержка 24/7</li>
                </ul>
            </div>
        </div>

        <div class="card">
            <div class="card-header bg-light">
                <h5 class="mb-0"><i class="fas fa-list me-2"></i>Похожие предложения</h5>
            </div>
            <div class="card-body">
                <div class="list-group list-group-flush">
                    {% for similar_offer in similar_offers %}
                    <a href="{% url 'offer_detail' similar_offer.id %}" class="list-group-item list-group-item-action">
                        <div class="d-flex justify-content-between">
                            <span>{{ similar_offer.item_name }}</span>
                            <strong>{{ similar_offer.price }} ₽</strong>
                        </div>
                        <small class="text-muted">{{ similar_offer.game.title }}</small>
                    </a>
                    {% empty %}
                    <div class="text-muted small">Нет похожих предложений</div>
                    {% endfor %}
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}